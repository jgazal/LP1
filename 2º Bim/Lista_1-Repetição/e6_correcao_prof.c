#include<stdio.h>

int main()
{
    int i, n, z;
    float s=0;

    printf("N: ");
    scanf("%f", &n);
    z = n;
    for(i=1, j=N; i<=z; i++)
    {
        s = s + i/n;
        n--;
    }
        printf("S = %.1f\n", S);
    
    return 0;
}
