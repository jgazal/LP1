25 Prepare um algoritmo que calcule o valor de H, sendo que ele é determinado pela série H = 1/1 + 3/2 + 5/3 + 7/4 +...+ 99/50.

inicio
    real: H, Dividendo, Divisor;
    inteiro: cont;
    cont <- 1;
    H <- 0;
    para cont de 1 até 50 passo 1 faça
       H <- Dividendo/Divisor;
       Dividendo <- Dividendo+2;
       Divisor <- Divisor+1;
       escreva("H = ",H);
       cont <- cont+1;
    fimpara;        
fim.
