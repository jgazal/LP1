3.3 Construa um algorítmo que verifique se um número fornecido pelo usuário é primo ou não.

inicio
    inteiro: N, V;
    caracter: P;
    leia(N);
    P <- "S";
    para V de N-1 até 2 passo -1 faça
        se(N mod V = 0)
            então P <- "N"
    fimpara;
    se(P = "S")
        então escreva("O número ", N, " é primo");
        senão escreva("O número ", N, " não é primo");
    fimse;
fim.
